
<div class="steam" style="left: 10%; width:8px; height:40px; animation-delay:0s;"></div>
<div class="steam" style="left: 20%; width:12px; height:50px; animation-delay:1s;"></div>
<div class="steam" style="left: 30%; width:10px; height:45px; animation-delay:0.5s;"></div>
<div class="steam" style="left: 40%; width:14px; height:55px; animation-delay:2s;"></div>
<div class="steam" style="left: 50%; width:9px; height:42px; animation-delay:1.5s;"></div>
<div class="steam" style="left: 60%; width:11px; height:48px; animation-delay:0.8s;"></div>
<div class="steam" style="left: 70%; width:13px; height:50px; animation-delay:2.5s;"></div>
<div class="steam" style="left: 80%; width:10px; height:46px; animation-delay:1.2s;"></div>
<div class="steam" style="left: 90%; width:12px; height:52px; animation-delay:0.3s;"></div>
<!-- nitip ya tin div diatas ~rapi -->

<nav>
    <div class="nav-left">
        <span class="points-display">
            ⭐ <%= (typeof point !== 'undefined' && point) ? point : '-' %> pts
        </span>

        <%
        const now = new Date();
        const options = {
            weekday: 'long',
            year: 'numeric',
            month: 'long',
            day: 'numeric',
            timeZone: 'Asia/Jakarta'
        };
        const hariIni = now.toLocaleDateString('id-ID', options) + " WIB";
        %>

        <span class="date-display">
            <%= hariIni %>
        </span>
    </div>

    <div class="nav-center">
        <% const isActive = (p) => (typeof page !== 'undefined' && page === p ? 'active' : '') %>

        <a href="/" class="<%= isActive('main') %>">MAIN</a>
        <a href="/foods" class="<%= isActive('foods') %>">FOODS</a>
        <a href="/drinks" class="<%= isActive('drinks') %>">DRINKS</a>
        <a href="/cart" class="<%= isActive('cart') %>">CART</a>
        <a href="/transactions" class="<%= isActive('history') %>">HISTORY</a>
        <a href="/forum" class="<%= isActive('forum') %>">FORUM</a>
        <% if (typeof role !== 'undefined' && role === "admin") { %>
            <a href="/admin/foods">ADMIN ONLY</a>
        <% } %>
        <a href="/profile" class="<%= isActive('profile') %>">PROFILE</a>
    </div>

    <div class="nav-right">
        <% 
            let displayName = '-';
            if (user) {
                displayName = (role === "admin" ? user.fullname_admin : user.fullname_customer) || '-';
            }
        %>

        <% if (user) { %>
            <span class="logged-as">
                LOGGED AS <%= displayName %>
            </span>
            <a href="/logout" class="btn-logout">LOGOUT</a>
        <% } else { %>
            <a href="/login" class="btn-logout">LOGIN</a>
        <% } %>
    </div>
</nav>

